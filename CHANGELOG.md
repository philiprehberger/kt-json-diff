# Changelog

## [0.1.0] - 2026-03-18

### Added

- `jsonDiff()` for comparing two JSON strings

- Change types: ADDED, REMOVED, MODIFIED

- JSONPath-based change paths

- `ignoreArrayOrder` and `ignorePaths` options

- `DiffResult` with filtering methods
